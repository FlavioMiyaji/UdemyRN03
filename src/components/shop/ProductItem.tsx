import React from 'react';
import { 
    View ,
    Text,
} from "react-native";

const ProductItem = (props: any) => {
    return (
        <View>

        </View>
    );
};

export default ProductItem;